'use strict';

exports.get = {
    description: 'Returns a specific role',
    handler: function (request, reply) {
        reply('');
    }
};

exports.getAll = {
    description: 'Returns all roles',
    handler: function (request, reply) {
        reply('');
    }
};

exports.post = {
    description: 'Create a new role',
    handler: function (request, reply) {
        reply('');
    }
};

exports.delete = {
    description: 'Delete a specific role',
    handler: function (request, reply) {
        reply('');
    }
};

exports.put = {
    description: 'Modify a specific role',
    handler: function (request, reply) {
        reply('');
    }
};
